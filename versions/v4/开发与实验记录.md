# v4 开发与实验记录

## 第 1 次迭代

### 目标

创建 `versions/v4`，从 `v1` 出发实现可靠性感知、文本锚定的动态融合。当前轮只完成代码与可运行入口，不伪造长时间实验结果。

### 阅读和审计结果

1. v1 的训练入口和测试入口：
   - 训练入口：`versions/v1/model/main.py`。
   - 训练器：`versions/v1/model/trainer.py::Trainer.train`。
   - 测试入口：同一入口加 `--only_test`，最终调用 `Trainer.evaluate`。
2. text logits 的生成函数：
   - `Trainer.predict` 中先设置 `self.model.module.set_adapter("lora_text")`。
   - 调用 `self.model(input_ids=input_ids, attention_mask=attention_mask, adapter_name="lora_text")` 得到 `scores`。
3. ID logits 的生成函数：
   - `use_gate` 分支中设置 `self.model.module.set_adapter("lora_cf")`。
   - 调用 `self.model(input_ids=item_input_ids, attention_mask=item_seq_mask, adapter_name="lora_cf", is_text=False)` 得到 `scores1`。
4. `confidence_fusion` 的文件、公式和调用位置：
   - 文件：`versions/v1/model/models/modules.py`。
   - `fusion_type="text"` 时：
     ```text
     logits_id_truth = sigmoid((logits_id - mean(logits_id)) / temperature)
     r_id = (logits_id - min(logits_id) + 1e-8) * logits_id_truth
     z_final = logits_text + alpha * r_id
     ```
   - 调用位置：`versions/v1/model/trainer.py::Trainer.predict` 和训练阶段若打开融合前损失。
5. 标签格式和训练 loss：
   - `Collator` 返回 `user_seq_data=(input_ids, attention_mask, target_ids, labels)`。
   - `labels` 是每个样本的 next item id。
   - 训练使用 `nn.CrossEntropyLoss()`，对 pooled logits 和 target ids 计算 CE。
6. validation/test 是全物品还是采样候选：
   - `Ranker` 直接在 `scores` 全维度上计算 target item rank，没有采样候选；是全物品评估。
7. checkpoint 保存与加载路径：
   - 最优模型保存到 `args.output_path/pytorch_model.bin`。
   - `evaluate` 在非 only_test 时从该路径加载。
8. 每个数据集的 `fusion_alpha` 配置来源：
   - `versions/run_common.sh` 中按数据集设置 `FUSION_ALPHA`。
   - Arts=0.7，Industrial=0.5，Musical=0.7，Prime=0.3，Video=0.5。
9. 当前结果文件和运行脚本：
   - 运行脚本：`versions/v1/run.sh`、`versions/run_common.sh`。
   - 历史结果在 `versions/v1/outputs/`。
10. 可以插入 logits 缓存和动态融合的位置：
    - 缓存位置：复用 v1 model/data/collator，在 eval forward 中分别取 text logits 和 id logits。
    - 动态融合位置：在 `confidence_fusion` 之后的残差形式上替换固定 `alpha`。
11. 数据集切分方式以及是否已有独立 calibration split：
    - 数据来自 `data/<dataset>/train.json`、`val.json`、`test.json`。
    - 当前没有独立 calibration split；v4 默认用 validation 缓存训练/校准 fuser，test 只评估。
12. Arts、Video 和小数据集的运行参数差异：
    - `versions/run_common.sh` 按数据集设置 epoch、lr、score dropout、batch size、KL 权重、fusion alpha 和 temperature。
    - Arts 使用 alpha=0.7，Video 使用 alpha=0.5 且 temperature=1.2；Prime 使用较低 alpha=0.3。

### 修改文件

- 新增 `versions/v4/`，从 `versions/v1/` 复制。
- 新增可靠性融合、特征、utility、metrics、cache、oracle、diagnostic、train/eval 脚本。
- 修改 `versions/run_common.sh`，加入 `v4` 分支，不改变 v1/v2/v3 分支。
- 修改 `.gitignore`，忽略 `versions/*/results/` 与 `versions/*/logs/`，避免提交缓存和实验产物。

### 核心实现

代码模块：

- `versions/v4/model/utility_label.py`
  - 复现 v1 `fusion_type="text"` 下的 ID 置信残差：
    `r_id = (logits_id - min(logits_id) + 1e-8) * sigmoid((logits_id - mean(logits_id)) / temperature)`。
  - 实现 `z_final = z_text + alpha * r_id`。
  - 实现 per-sample CE、utility value 和 utility label。
- `versions/v4/model/reliability_features.py`
  - 实现 `text_entropy`、`id_entropy`、`text_margin`、`id_margin`、`branch_jsd`、`topk_overlap`、`history_length`、`history_pop_mean`、`history_pop_std`、`text_id_score_corr`。
  - 使用稳定 softmax，并对 NaN/Inf 做清理。
  - 提供基于训练集的 item popularity 统计。
- `versions/v4/model/reliability_fusion.py`
  - 实现 `FeatureStandardizer`。
  - 实现 `ContextGate`。
  - 实现残差安全参数化：
    `alpha = clamp(alpha0 + rho * tanh(gate(features)), 0, alpha_max)`。
  - 保存 fuser checkpoint 时包含标准化状态、feature schema 和 alpha 配置。
- `versions/v4/model/ranking_metrics.py`
  - 实现全物品 target rank、NDCG/Recall/MRR 和 alpha 分布统计。
- `versions/v4/scripts/cache_fusion_data.py`
  - 加载冻结 v1 checkpoint。
  - 分别前向 text/id 分支。
  - 缓存 logits、labels、user_ids、history/pop 特征、loss 和 utility label。
  - 支持 train/valid/test/calibration split。
- `versions/v4/scripts/analyze_oracle.py`
  - 实现 Oracle-Select。
  - 实现离散网格 Oracle-Alpha。
  - 输出 alpha 分布与分桶统计。
- `versions/v4/scripts/analyze_reliability.py`
  - 使用线性分类器验证 utility 可预测性。
  - 输出 AUC、Accuracy、F1、Brier、正例比例和线性系数。
- `versions/v4/scripts/train_fuser.py`
  - 只读取缓存训练 fuser，不更新主模型。
  - 损失为 `L_rank + lambda_u L_utility + lambda_r L_shrink`。
  - 根据 valid NDCG@10 保存最佳 fuser。
- `versions/v4/scripts/evaluate_v4.py`
  - 比较 text-only、id-only、v1 fixed fusion、v4 context fusion、Oracle-Select、Oracle-Alpha。
  - 输出 harm_rate、benefit_rate、average_target_rank_gain 和 alpha 统计。
- `versions/v4/tests/smoke_v4.py`
  - CPU 合成测试：alpha=0 严格退化、特征无 NaN/Inf、Oracle 可跑、fuser 保存恢复、缓存读写一致。

### 运行命令

静态检查：

```bash
conda run -n hdrec python -m py_compile \
  versions/v4/model/reliability_features.py \
  versions/v4/model/reliability_fusion.py \
  versions/v4/model/utility_label.py \
  versions/v4/model/ranking_metrics.py \
  versions/v4/scripts/common.py \
  versions/v4/scripts/cache_fusion_data.py \
  versions/v4/scripts/analyze_oracle.py \
  versions/v4/scripts/analyze_reliability.py \
  versions/v4/scripts/train_fuser.py \
  versions/v4/scripts/evaluate_v4.py \
  versions/v4/tests/smoke_v4.py
```

CPU smoke test：

```bash
conda run -n hdrec python versions/v4/tests/smoke_v4.py
```

真实数据建议命令（已在“v4 修复迭代 1”中修正；以下保留基础缓存与评估入口，fuser 必须使用独立 calibration 文件）：

```bash
# 1. 缓存冻结 v1 的双分支预测
conda run -n hdrec python versions/v4/scripts/cache_fusion_data.py \
  --dataset Industrial_and_Scientific \
  --split valid \
  --checkpoint_path versions/v1/outputs/Industrial_and_Scientific/deepseek-ai-DeepSeek-R1-Distill-Llama-8B_all_klw0.7_fa0.5/pytorch_model.bin \
  --cache_path versions/v4/results/cache/Industrial_and_Scientific/valid.pt \
  --overwrite

# 2. 运行 Oracle 和 utility 诊断
conda run -n hdrec python versions/v4/scripts/analyze_oracle.py \
  --cache_path versions/v4/results/cache/Industrial_and_Scientific/valid.pt \
  --output_json versions/v4/results/cache/Industrial_and_Scientific/valid.oracle.json

conda run -n hdrec python versions/v4/scripts/analyze_reliability.py \
  --cache_path versions/v4/results/cache/Industrial_and_Scientific/valid.pt \
  --output_json versions/v4/results/cache/Industrial_and_Scientific/valid.utility.json

# 3. 按用户拆分 calibration，禁止复用同一 cache 训练和早停
conda run -n hdrec python versions/v4/scripts/split_calibration.py \
  --input_cache versions/v4/results/cache/Industrial_and_Scientific/valid.pt \
  --train_output versions/v4/results/cache/Industrial_and_Scientific/calibration_train.pt \
  --valid_output versions/v4/results/cache/Industrial_and_Scientific/calibration_valid.pt \
  --seed 42

# 4. 训练 Context Gate
conda run -n hdrec python versions/v4/scripts/train_fuser.py \
  --train_cache versions/v4/results/cache/Industrial_and_Scientific/calibration_train.pt \
  --valid_cache versions/v4/results/cache/Industrial_and_Scientific/calibration_valid.pt \
  --output_path versions/v4/results/fuser/Industrial_and_Scientific/fuser.pt \
  --device cuda

# 5. 评估 v4
conda run -n hdrec python versions/v4/scripts/evaluate_v4.py \
  --cache_path versions/v4/results/cache/Industrial_and_Scientific/test.pt \
  --fuser_path versions/v4/results/fuser/Industrial_and_Scientific/fuser.pt \
  --output_json versions/v4/results/fuser/Industrial_and_Scientific/test.eval.json \
  --device cuda
```

### 实际测试结果

- 默认 `/home/miniconda/bin/python` 缺少 `torch`，首次 smoke test 失败：
  - `ModuleNotFoundError: No module named 'torch'`
  - 处理：改用 `conda run -n hdrec`。
- `conda run -n hdrec python -m py_compile ...` 通过。
- `conda run -n hdrec python versions/v4/tests/smoke_v4.py` 通过：
  - 输出：`v4 smoke test passed`
- 脚本 help 均可正常打开：
  - `cache_fusion_data.py --help`
  - `analyze_oracle.py --help`
  - `analyze_reliability.py --help`
  - `train_fuser.py --help`
  - `evaluate_v4.py --help`
- 合成缓存端到端测试：
  - `analyze_oracle.py` 通过并输出 `/tmp/hdrec_v4_oracle.json`。
  - `analyze_reliability.py` 通过并输出 `/tmp/hdrec_v4_utility.json`。
  - `train_fuser.py` 通过并输出 `/tmp/hdrec_v4_fuser.pt`。
  - `evaluate_v4.py` 初次失败，因为 fuser checkpoint extra 中含 tensor，JSON 不可序列化。
  - 修复 `scripts/common.py::save_json` 后，`evaluate_v4.py` 通过并输出 `/tmp/hdrec_v4_eval.json`。

### 未运行项目

- 未运行完整 GPU 缓存、训练和评估。
- 未进行长时间数据集实验。
- 未验证真实 v1 checkpoint 路径是否完全覆盖所有数据集；需要实际缓存时逐数据集检查。

### 已知问题

- 当前仓库没有独立 calibration split；默认使用 valid split 训练 fuser，test split 仅评估。
- `cache_fusion_data.py` 当前将 split 缓存为单个 `.pt` 文件；超大数据集如显存/内存不足，需要继续扩展 shard 写入。
- 当前 fuser 训练示例使用 valid 同时训练和验证，仅用于调通流程；正式实验应使用独立 calibration 或 out-of-fold 方案。

### 下一步

1. 先用小数据集或 `--max_samples` 跑真实 checkpoint 缓存，验证 v1 checkpoint 加载路径。
2. 对 Arts 和 Video 分别跑 valid/test 缓存。
3. 先看 Oracle-Select、Oracle-Alpha 和 utility AUC，再决定是否正式训练 Context Gate。
4. 若 Oracle 空间不足或 utility AUC 接近随机，应暂停 v4.1，不继续堆 Candidate Gate。

## v4 修复迭代 1

### 目标

修复 Context Gate 的监督链路、utility 标签、calibration 切分和 checkpoint 恢复约束；本轮只进行静态测试与 256 样本 Industrial smoke，不启动正式实验。

### 审计发现

1. `train_fuser.py` 使用独立 `utility_head` 预测 utility，utility BCE 的梯度不会进入生成 alpha 的 `ReliabilityFuser.gate`，因此“边际效用监督动态权重”没有真正实现。
2. `utility_label.py` 按 `text + 1.0 * id_residual` 生成标签，而 Industrial 的 v1 固定融合使用 `alpha0=0.5`，监督目标与比较基线不一致。
3. 旧示例将同一份 `valid.pt` 同时作为 fuser train/valid，存在模型选择泄漏，无法作为论文实验设置。
4. `scripts/common.py` 使用 `strict=False` 加载 v1 checkpoint，只打印 incompatible keys，结构不匹配时仍可能继续缓存并污染后续结果。
5. fuser checkpoint 未保存 `hidden_dim`、`dropout`、`dataset`、`seed`，加载时依赖命令行重新指定网络结构。
6. 现有 smoke 脚本覆盖部分基础性质，但缺少 utility-to-gate 梯度、按用户 calibration 隔离、dynamic=false 等价性和 checkpoint schema 的独立回归测试。
7. 当前 `hdrec` 环境未安装 `pytest`；Industrial 实际 v1 checkpoint 为 `versions/v1/outputs/Industrial_and_Scientific/deepseek-ai-DeepSeek-R1-Distill-Llama-8B_all_klw0.7_fa0.5/pytorch_model.bin`。

这些问题会使 gate 的方法描述与真实优化目标不一致、验证分数偏乐观，或在加载错误权重时仍产出缓存，均直接影响实验可复现性与论文可信度。

### 修改计划

1. 让同一个 gate logit 同时生成 utility probability 和 alpha；utility BCE 直接反传到 alpha gate，并记录梯度范数。
2. 按当前数据集 `alpha0` 生成 utility 标签并输出标签平衡统计。
3. 新增按用户、固定种子、尽量分层的 calibration 切分脚本，保存样本索引和统计。
4. 对 v1 checkpoint 启用 incompatible-key 硬失败；完善 fuser checkpoint 自描述字段和无命令行结构恢复。
5. 增加独立 pytest 测试并运行 CPU 测试；通过后再以 `screen` 启动 Industrial 256 样本 cache、Oracle 和 utility analysis。

说明：总损失采用 `rank_loss + lambda_u * utility_loss + lambda_r * shrink_loss`。原修复提示中的减号若用于最小化会鼓励增大 BCE 和 shrink loss，与设计目标相反。

### 修改文件

- 修改 `model/reliability_fusion.py`：移除 gate 末端 Tanh，统一 utility 与 alpha 的 gate logit；补齐自描述 checkpoint。
- 修改 `model/utility_label.py`：utility 按数据集 `alpha0` 计算，并提供正负比例、均值和标准差统计。
- 修改 `scripts/common.py`：v1 checkpoint 出现 missing/unexpected keys 时硬失败，并打印路径、模型类和配置。
- 修改 `scripts/cache_fusion_data.py`：缓存 `utility_alpha0`、真实 checkpoint 路径和 utility 统计。
- 新增 `scripts/split_calibration.py`：固定种子、优先用户隔离、兼顾 utility 比例的 calibration 拆分。
- 修改 `scripts/analyze_reliability.py`：支持独立 `--valid_cache` 并拒绝用户重叠。
- 修改 `scripts/train_fuser.py`：删除独立 utility head，BCE 直接监督 alpha gate；增加梯度范数和 utility 指标。
- 修改 `scripts/evaluate_v4.py`：完全从 checkpoint 恢复结构，并检查数据集和特征 schema。
- 新增 `scripts/run_industrial_smoke.sh`：GPU 7 上的 256 样本 cache、split、Oracle、utility 后台流水线。
- 新增 `tests/test_gate_gradient.py`、`test_alpha_zero.py`、`test_dynamic_false.py`、`test_calibration_split.py`、`test_checkpoint.py`、`test_feature_nan.py` 和测试路径配置。

### 核心实现

```text
gate_logit = gate(normalized_features)
utility_prob = sigmoid(gate_logit)
alpha = clamp(alpha0 + rho * (2 * utility_prob - 1), 0, alpha_max)
final_logits = text_logits + alpha * id_residual
```

训练损失为：

```text
loss = rank_loss + utility_loss_weight * BCEWithLogits(gate_logit, utility_label)
                 + shrink_loss_weight * mean((alpha - alpha0)^2)
```

每轮首个 batch 分别记录 `grad(alpha_gate)` 与 `grad(utility_loss -> gate)`。后者由 `autograd.grad(utility_loss, gate.parameters())` 独立测量，用于证明 utility 监督确实进入 alpha gate。

### 运行命令

```bash
conda run -n hdrec python -m pytest versions/v4/tests -q

screen -L -Logfile /data/lgd/HDRec/versions/v4/logs/industrial_smoke.log \
  -dmS hdrec_v4_smoke bash /data/lgd/HDRec/versions/v4/scripts/run_industrial_smoke.sh
```

### 实际测试结果

- `python -m py_compile` 已通过所有本轮修改的 Python 文件。
- 在 `hdrec` 环境安装 `pytest==8.4.2` 后，`conda run -n hdrec python -m pytest versions/v4/tests -q` 实际通过：`7 passed in 1.62s`。
- 原有 `conda run -n hdrec python versions/v4/tests/smoke_v4.py` 实际通过。
- GPU 7 检查时为空闲状态：A800 80GB，显存占用 0 MiB。
- Industrial 256 样本诊断已通过 detached screen `hdrec_v4_smoke` 实际完成，日志为 `logs/industrial_smoke.log`：
  - v1 checkpoint 严格加载成功：`missing_keys=[]`、`unexpected_keys=[]`。
  - cache 共 256 个样本、4971 个物品；`alpha0=0.5`。
  - utility 正例比例 0.5078、负例比例 0.4922、均值 -0.3169、标准差 1.2954。
  - calibration train/valid 为 204/52 个样本，分别含 204/52 个用户，用户无交叉；正例比例分别为 0.5098/0.5000。
  - text-only NDCG@10=0.24672，v1 fixed NDCG@10=0.24499，Oracle-Select NDCG@10=0.25549，Oracle-Alpha NDCG@10=0.26034。
  - Oracle-Alpha 选择 alpha=0 的比例为 0.4922，说明样本级禁用 ID 修正存在明显需求。
  - 独立 calibration-valid 上线性 utility AUC=0.6317、Accuracy=0.6346、F1=0.5778、Brier=0.2336。

以上均为固定前 256 个 validation 样本的工程 smoke 结果，只用于验证方向和代码链路，不作为论文正式结果。

### 未运行项目

- 未运行 Industrial 全量缓存、Context Gate 正式训练或 test 评估。
- 未运行 Arts、Video 或其他数据集。
- 未实现 Candidate Gate、v5 对齐、LoRA 或 HDRec 主模型改动。

### 已知问题

- 256 样本 smoke 只验证工程链路，不能作为论文指标。
- 当前 cache 仍是单个 `.pt` 文件；正式大数据集运行前仍需根据内存占用决定是否实现分片。
- 当前 utility 平衡、Oracle 上限和 utility AUC 仅来自固定前 256 个 validation 样本，仍需在预注册的完整 calibration 协议下复核。

### 下一步

1. 当前 smoke 的 Oracle 和 utility AUC 初步支持继续，但下一步只训练 256 样本级 Context Gate 做工程调试。
2. 对动态 gate 与重新调优的固定 alpha 做同一 calibration/test 协议下的公平比较。
3. 小规模 gate 的梯度、alpha 分布和独立 validation 指标正常后，再制定 Industrial 正式实验协议。

## v4 Industrial 全量诊断迭代

### 当前 commit

- 开始本轮工作时：`55f6a60 修复v4`。
- `git pull --ff-only` 返回 `Already up to date.`，工作区干净。

### 本轮目标

1. 修复 Context Gate 初始化，使新建模型严格从 v1 固定 `alpha0` 开始。
2. 完善 train/valid cache 与 fuser/cache 的硬兼容性校验。
3. 同时缓存和分析 CE Utility、Rank Utility、best global alpha 与 Utility×Oracle-alpha 方向。
4. 完成测试和内存预估后，仅在 GPU 7 启动 Industrial 全量 validation 诊断，不运行 test 或正式 Context Gate。

### 审计发现

1. Context Gate 最后一层为随机初始化，新建 fuser 的 `gate_logit` 不保证为 0，初始 alpha 会随机偏离 `alpha0`。
2. `train_fuser.py` 目前只检查 dataset、feature names、文件路径和用户交集，未检查 cache version、feature schema、基础 checkpoint、alpha/temperature 与候选维度。
3. fuser checkpoint 未保存 feature schema、基础 checkpoint、fusion temperature 和 utility target，评估无法证明 fuser 与 cache 来自同一冻结模型和同一监督定义。
4. cache 仅保存 CE Utility；尚无目标物品 rank gain、rank improve/harm/tie 标签。
5. Oracle 步长仍默认为 0.1，未输出全局 alpha 曲线、calibration-valid 最佳 global alpha、逐样本 best alpha 和 Utility×alpha 交叉表。
6. fuser 训练日志未拆分 rank/utility/shrink loss，也缺少 Rank Utility AUC 与 alpha 边界占比。

### 修改文件

- 修改 `model/reliability_fusion.py`：Context Gate 输出层 weight/bias 零初始化；fuser checkpoint 增加 feature schema、基础 checkpoint、fusion temperature 和 utility target。
- 新增 `model/cache_compatibility.py`：统一校验 train/valid cache 与 fuser/cache 的数据来源、结构、浮点配置、候选维度和用户隔离。
- 修改 `model/utility_label.py`：新增 target rank gain 与 improve/harm/tie 标签。
- 修改 `model/ranking_metrics.py`：alpha 汇总增加 floor/ceiling rate。
- 修改 `scripts/cache_fusion_data.py`：同时缓存 CE Utility、Rank Utility 和兼容旧字段。
- 修改 `scripts/analyze_reliability.py`：分别训练和报告 CE、rank improved-vs-non-improved，以及样本足够时 improved-vs-harmed 线性诊断。
- 修改 `scripts/analyze_oracle.py`：alpha 网格改为 0.05；新增全局 alpha 曲线、best global、逐样本 best alpha、CE/Rank Utility×Oracle-alpha 方向与交叉表。
- 修改 `scripts/train_fuser.py`：接入统一 cache 校验，补齐四项训练 loss、两种 valid Utility AUC、alpha 边界率和梯度日志。
- 修改 `scripts/evaluate_v4.py`：评估前强制校验 fuser 与 cache 的完整来源和配置。
- 新增 `scripts/summarize_diagnostics.py`：汇总核心指标并逐条判断 Gate 启动条件。
- 新增 `scripts/run_industrial_full_diagnostic.sh`：GPU 7 全量 validation 诊断流水线，不包含 Gate 或 test。
- 新增 `tests/test_gate_initialization.py`、`test_cache_compatibility.py`、`test_rank_utility.py`，并更新 checkpoint/smoke 测试。

### 测试结果

- `conda run -n hdrec python -m pytest versions/v4/tests`：实际通过，`13 passed in 1.44s`。
- `conda run -n hdrec python -m compileall -q versions/v4`：实际通过。
- `bash -n versions/v4/scripts/*.sh`：实际通过。
- `git diff --check`：实际通过。
- 原有 `conda run -n hdrec python versions/v4/tests/smoke_v4.py`：实际通过。

### Industrial 全量 cache 内存预估

- validation 样本数：10623；物品/候选数：4971。
- 上次真实 smoke 确认两路 logits dtype 均为 `torch.bfloat16`，features 为 FP32。
- 两路 BF16 logits：约 201.44 MiB；新增 utility/feature/label 元数据约 1 MiB，预计最终 `.pt` 约 203 MiB。
- 保留 batch tensor 列表并执行 `torch.cat` 时，CPU 峰值估计约 405 MiB；按 FP32 意外上界估计也低于 0.8 GiB。
- fuser 训练将 text、ID 和 residual 全部转 FP32 搬入 GPU，三者合计约 604.73 MiB，不含小量中间张量。
- 预检资源：CPU 可用 397 GiB；数据盘可用 9.8 TiB；GPU 7 为 A800 80GB，预检时使用 0 MiB、空闲约 81154 MiB。
- 结论：单文件 cache、`torch.cat` 和后续单配置 fuser 均有充足余量，本轮无需实现分片。

### 运行任务

- 已通过 detached screen 启动 Industrial 全量 validation 诊断：
  - screen 名：`hdrec_v4_ind_diag`
  - screen PID：`836109`
  - 流水线 bash PID：`836110`
  - 启动时 cache Python PID：`836222`
  - 日志：`versions/v4/logs/industrial_full_diagnostic.log`
  - 结果目录：`versions/v4/results/full_diagnostic/Industrial_and_Scientific/`
  - 启动后确认 GPU 7 占用约 20320 MiB，模型加载和 cache 进程均已实际运行。
- 流水线仅包含 validation cache、Oracle、calibration split、CE/Rank Utility、best-global、方向分析和 summary；不包含 Gate、test、Arts 或 Video。

### 实验结果

- 首次全量流水线完成 cache、validation Oracle 和 calibration 拆分后失败：
  - 失败位置：`scripts/analyze_reliability.py` 启动阶段。
  - 错误：`ModuleNotFoundError: No module named 'cache_compatibility'`。
  - 原因：脚本在导入 `common.py`、由其加入 `versions/v4/model` 到 `sys.path` 之前，就先导入了 `cache_compatibility`；pytest 的 `conftest.py` 预先设置路径，因此单元测试未暴露该入口问题。
  - 修复：调整导入顺序，并增加从现有 calibration cache 继续执行的 `run_industrial_full_diagnostic_resume.sh`，不重复模型前向。
- 已完成的全量 validation 初步结果：
  - text-only NDCG@10=0.148849；v1 fixed(alpha0=0.5) NDCG@10=0.145166；best global alpha=0.0。
  - Oracle-Alpha NDCG@10=0.160651，说明样本级选择仍有空间，但全局上 ID 融合有害。
  - CE Utility 正例中 `best_alpha > alpha0` 的比例仅 0.410680，而 `best_alpha < alpha0` 为 0.578398，`positive_direction_supported=False`。
  - Rank Utility 的正负方向均得到支持。由于当前 Gate 使用 CE Utility BCE，方向条件已经失败，本轮禁止启动 Context Gate。

### 下一步判断

只有全量诊断完成并满足预设条件后，才允许启动单配置 Context Gate；本轮长任务启动后不等待、不运行 Gate。

### 全量诊断最终结果

首次流水线因入口导入顺序中止后，使用 `hdrec_v4_ind_resume` 从已生成的 calibration cache 继续；续跑于 `2026-07-19T12:10:52+08:00` 正常结束，未重复模型前向。

Calibration-valid 核心结果：

- text-only NDCG@10：0.156986。
- v1 fixed alpha0=0.5 NDCG@10：0.154750。
- best global alpha：0.0，NDCG@10：0.156986。
- Oracle-Alpha NDCG@10：0.169240；相对 best global 差值：0.012255。
- CE Utility AUC：0.608617。
- Rank Utility improved-vs-non-improved AUC：0.610983。
- Rank Utility improved-vs-harmed（排除 tie）AUC：0.519506。
- CE/Rank 标签一致率：0.561560。
- Rank improved/harmed/tie 比例：0.448778 / 0.402726 / 0.148496。

Utility 与 Oracle alpha 方向：

- CE Utility=1 时，`best_alpha > alpha0` 比例为 0.418689，`best_alpha < alpha0` 比例为 0.576456，正向条件失败。
- Rank Utility=1 时，`best_alpha > alpha0` 比例为 0.855497；Rank Utility=0 时，`best_alpha < alpha0` 比例为 0.983802，Rank 方向成立。
- 当前 Context Gate 的 BCE 监督目标是 CE Utility，因此不能用 Rank 方向成立替代 CE 方向失败；否则将改变本轮预注册方法。

Gate 条件逐项结论：

- Oracle 高于 best global：通过。
- best global 不能解释大部分 Oracle 收益：通过。
- CE Utility AUC >= 0.60：通过。
- Rank Utility AUC >= 0.60：通过（仅 improved-vs-non-improved；排除 tie 后接近随机）。
- CE Utility 与 alpha 调整方向一致：失败。
- Rank Utility 与 alpha 调整方向一致：通过。
- 标签未坍缩：通过。
- 内存安全：通过。

最终 `gate_training_allowed=false`，本轮未启动 Context Gate、test、Arts、Video、Candidate Gate 或 v5。完整结果见 `results/full_diagnostic/Industrial_and_Scientific/summary.json` 与 `summary.md`。
